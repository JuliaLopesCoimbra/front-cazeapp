NEXT_PUBLIC_API_URL=https://api.n1app.com.br
# Web Push (notificações no celular/PC). Gerar com: pip install py-vapid && python -m py_vapid
# Coloque aqui a chave PÚBLICA (a mesma que você usa no back como VAPID_PUBLIC_KEY, ou gerada junto com VAPID_PRIVATE_KEY).
NEXT_PUBLIC_VAPID_PUBLIC_KEY=